// @ds-preview generated 5ccc9248b6cb — delete this line to keep your edits across re-syncs.
import { MediaUploader } from '@repo/ui';

export const Variants = () => (
  <>
    <MediaUploader purpose={"CAMPAIGN_PHOTO"} initial={[]} onChange={()=>null} apiClient={{}} />
    <MediaUploader purpose={"JOB_PROOF"} initial={[]} onChange={()=>null} apiClient={{}} />
  </>
);
