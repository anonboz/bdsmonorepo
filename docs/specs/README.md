# docs/specs

One spec per feature. Copy `_template.md` to `<feature-slug>.md` before writing
code, get it reviewed, then implement against it.

Naming: `phase<n>-<feature-slug>.md` — e.g., `phase1-auth.md`,
`phase2-bill-generation.md`.

The PR that implements a feature must link the spec.
