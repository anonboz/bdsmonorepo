// Conventional commits for the house-renting monorepo. Run by the repo-root
// .husky/commit-msg hook (which cd's into house-renting).
module.exports = {
  extends: ["@commitlint/config-conventional"],
};
